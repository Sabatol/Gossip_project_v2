<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8">
    <title>The Gossip Project</title>
    <!-- On peut avoir d'autres méta-données ici -->
  </head>
  <body>
      <br/>
      <br/>
    <h1><strong>PAGE D'ACCUEIL</strong></h1>
      <br/>
      <br/>
      <h3>Bienvenue !</h3>
      <br/>
      <br/>
    <div class="">
    <% @gossips.each do |gossip|%>
        <div class="card p-0">
          <div class="card-body">
          <blockquote class="blockquote mb-0">
            <p><%= gossip.content%></p>

          </blockquote>
            
            <footer class="blockquote-footer"><%= gossip.user[:first_name]%><cite title="Source Title"> <%= gossip.title%></cite>

      <br/>
        <div class="d-flex justify-content-center">
          <%= link_to "Accéder au gossip", gossip_path(gossip.id), :class =>"btn btn-md btn-info" %>
          <br/>
          <%= link_to "Accéder à la ville", city_path(gossip.user.city), :class =>"btn btn-md btn-info" %>
          <br/>
          <%= link_to "Accéder au profil de l'utilisateur", user_path(gossip.user), :class =>"btn btn-md btn-info" %>
        </div>
      <br/>
      <br/>
            <div class="d-flex justify-content-end">
              <%= link_to "#{gossip.comments.count} commentaires", gossip_path(gossip.id), :class =>"btn btn-outline-info"  %>
              <br/>
  <!--Partie like -->
              <% if current_user %>
                <% if get_like(current_user, gossip) %>
                <span><%= link_to  "", gossip_like_path(gossip.id, get_like(current_user, gossip).id), method: :delete, class:"fas fa-thumbs-up fa-2x" %></span>
                <% else %>
                <span><%= link_to  "", gossip_likes_path(gossip.id), method: :create, class:"far fa-thumbs-up fa-2x" %></span>
                <% end %>
              <% else %>
                <i class="far fa-thumbs-up fa-2x"></i>
              <% end %>
              <br/>
  <!--fin partie like-->
              <br/>
              <h4><%="(#{gossip.likes.count})"%></h4>
            </div>
            </footer>
          </div>
        </div>
      <br/>
      <br/>
    <% end %>
  </body>
</html>