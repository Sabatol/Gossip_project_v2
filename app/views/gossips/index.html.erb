<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8">
    <title>The Gossip Project</title>
    <!-- On peut avoir d'autres méta-données ici -->
  </head>
  <body>
      <br/>
      <br/>
    <h1><strong>PAGE D'ACCUEIL</strong></h1>
      <br/>
      <br/>
      <h3>Bienvenue !</h3>
      <br/>
      <br/>
    <div class="">
    <% @gossips.each do |gossip|%>
        <div class="card p-0">
        <div class="card-header">
          Potin n°<%= gossip.id%>
        </div>
          <div class="card-body">
          <blockquote class="blockquote mb-0">
            <p><%= gossip.content%></p>
            
            <footer class="blockquote-footer"><%= gossip.user[:first_name]%><cite title="Source Title"> <%= gossip.title%></cite>
              <div class="d-flex justify-content-end">
                <%= link_to "#{gossip.comments.count} commentaires", gossip_path(gossip.id), :class =>"btn btn-outline-info"  %>
              </div>
            </footer>
          </blockquote>
          </div>
        </div>
      <br/>
      <br/>
        <div class="d-flex justify-content-center">
          <%= link_to "Accéder au gossip", gossip_path(gossip.id), :class =>"btn btn-lg btn-info" %>
          <br/>
          <%= link_to "Accéder à la ville", city_path(gossip.user.city), :class =>"btn btn-lg btn-info" %>
          <br/>
          <%= link_to "Accéder au profil de l'utilisateur", user_path(gossip.user), :class =>"btn btn-lg btn-info" %>
        </div>
      <br/>
      <br/>
      <br/>
      <br/>
    <% end %>
  </body>
</html>